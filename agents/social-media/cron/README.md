# social-media cron

Do not enable jobs until Meta tokens are in the Hermes `social-media` profile and a real approved post exists in Liba OS.

Suggested later watch:

```
python agents/social-media/scripts/worker.py --watch --interval 30
```
